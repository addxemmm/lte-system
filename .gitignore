# ---> Go
bin/
*.exe

# ---> Data / runtime (never commit live state)
/data/
var/log/*.log
var/log/*.pcap
*.pcap
!configs/*.example

# ---> Firmware binaries are intentionally tracked (small .bin), but ignore vendor zips
firmware/**/*.zip
*.zip

# ---> Legacy dumps (already removed from index; keep them out)
legacy-python-workspace/log/
legacy-python-workspace/__pycache__/
*.backtrace.crash
legacy-python-workspace/conf/*_run.conf

# ---> OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
