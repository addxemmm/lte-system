# legacy 历史存档 Legacy Archive

2023 年原始中文文档（存档saved profile，只读）。
Original 2023 Chinese docs (saved profile, read-only).

- `LTESystem README.pdf` — 9 个接口的原始完整说明（含 `message_id` 表），Go 版已全部兼容，实现细节以 `docs/API.md` 为准 / Original complete spec of the 9 APIs (including `message_id` table); the Go version is fully compatible, implementation details follow `docs/API.md`
- `LTE-System简要说明.md` / `.pdf` — 系统简介与架构初稿，现架构图见 `docs/images/` / Early system intro and architecture draft; current diagrams are in `docs/images/`

这些文件不再更新。如发现新旧文档冲突，以 `docs/` 下的 Markdown 为准。
These files are no longer updated. In case of conflict between old and new docs, the Markdown files under `docs/` prevail.

---
**导航 Navigation:** [文档索引 Docs](../README.md)
